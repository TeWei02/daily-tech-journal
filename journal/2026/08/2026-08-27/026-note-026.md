# JWT 校验的必查项：落到真实场景的改造

**日期**：2026-08-27　**领域**：安全　**编号**：26/115

## 背景

把它放进真实流程，通常要补上配置、日志与失败分支。这里围绕「JWT 校验的必查项」做一次整理，重点放在能直接落地的部分。

## 要点

- 校验签名算法、签发者、受众与过期时间
- 不要信任未校验的头部信息
- 改造后别忘了补一次端到端验证。

## 示例

```python
jwt.decode(token, key, algorithms=['RS256'], audience='api')
```

## 易错点

- 接受 alg=none 是经典漏洞

## 小结

JWT 校验的必查项 在落到真实场景的改造这一面，结论可以归纳为：把前提写清楚、把失败路径覆盖到，剩下的就是按数据调优。
