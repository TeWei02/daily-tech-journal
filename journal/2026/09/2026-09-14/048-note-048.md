# 分页查询的深翻页优化：基础用法与最小示例

**日期**：2026-09-14　**领域**：数据库　**编号**：48/115

## 背景

先写出能跑通的最小示例，把输入输出钉死，再谈扩展。这里围绕「分页查询的深翻页优化」做一次整理，重点放在能直接落地的部分。

## 要点

- LIMIT 大偏移量会扫描并丢弃前 N 行
- 游标分页用上一页最后一条的键做条件
- 最小示例留着，回归时可直接复用。

## 示例

```sql
SELECT * FROM t WHERE id > 100000 ORDER BY id LIMIT 20;
```

## 易错点

- 游标分页要求排序键唯一且稳定

## 小结

分页查询的深翻页优化 在基础用法与最小示例这一面，结论可以归纳为：把前提写清楚、把失败路径覆盖到，剩下的就是按数据调优。
