# 特征穿越与数据泄漏：基础用法与最小示例

**日期**：2026-09-08　**领域**：数据　**编号**：76/115

## 背景

先写出能跑通的最小示例，把输入输出钉死，再谈扩展。这里围绕「特征穿越与数据泄漏」做一次整理，重点放在能直接落地的部分。

## 要点

- 训练特征只能使用预测时刻之前可获得的信息
- 时间序列切分不要随机打乱
- 最小示例留着，回归时可直接复用。

## 示例

```python
train = df[df.ts < cutoff]
valid = df[df.ts >= cutoff]
```

## 易错点

- 用全局统计量做标准化会造成泄漏

## 小结

特征穿越与数据泄漏 在基础用法与最小示例这一面，结论可以归纳为：把前提写清楚、把失败路径覆盖到，剩下的就是按数据调优。
