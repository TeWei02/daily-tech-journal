# Trie 的空间与查询权衡：实现原理与内部机制

**日期**：2026-09-08　**领域**：算法　**编号**：99/115

## 背景

知道底层怎么走，行为异常时才能判断卡在哪一层。这里围绕「Trie 的空间与查询权衡」做一次整理，重点放在能直接落地的部分。

## 要点

- 用字典嵌套实现简单但内存开销大，数组实现更紧凑
- 前缀查询与自动补全共用同一结构
- 原理清楚后，参数上限与限制条件基本能自己推导。

## 示例

```python
def insert(root, word):
    node = root
    for ch in word:
        node = node.setdefault(ch, {})
    node['#'] = True
```

## 易错点

- 计数场景要在节点上保存额外字段

## 小结

Trie 的空间与查询权衡 在实现原理与内部机制这一面，结论可以归纳为：把前提写清楚、把失败路径覆盖到，剩下的就是按数据调优。
