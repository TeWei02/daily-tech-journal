# 权限位与 ACL 的叠加：基础用法与最小示例

**日期**：2026-09-24　**领域**：Linux　**编号**：35/104

## 背景

先写出能跑通的最小示例，把输入输出钉死，再谈扩展。这里围绕「权限位与 ACL 的叠加」做一次整理，重点放在能直接落地的部分。

## 要点

- 目录的 x 位决定能否进入，读目录内容需要 r
- ACL 生效后 group 权限会显示为 mask
- 最小示例留着，回归时可直接复用。

## 示例

```bash
setfacl -m u:deploy:rx /srv/app
getfacl /srv/app
```

## 易错点

- umask 会影响新建文件，但不会回收已有权限

## 小结

权限位与 ACL 的叠加 在基础用法与最小示例这一面，结论可以归纳为：把前提写清楚、把失败路径覆盖到，剩下的就是按数据调优。
